import { QueryParamsFormaterPipe } from './query-params-formater.pipe';

describe('QueryParamsFormaterPipe', () => {
  it('create an instance', () => {
    const pipe = new QueryParamsFormaterPipe();
    expect(pipe).toBeTruthy();
  });
});
